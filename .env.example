# V1 uses your locally authenticated Sprite CLI.
# If `sprite list` works in your terminal, the dashboard can read the same account.

# Future hosted/token mode could use something like:
# SPRITES_API_TOKEN=
